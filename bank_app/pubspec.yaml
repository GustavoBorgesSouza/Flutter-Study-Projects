name: favoriter
description: First project using flutter, a list of random words

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "0.12.0"
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "asset/icons/icon_firstapp.png"

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true